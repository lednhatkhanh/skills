interface:
  display_name: "Use nuqs"
  short_description: "Type-safe React URL query state with nuqs"
  default_prompt: "Use $use-nuqs to implement type-safe URL query state with React, TypeScript, and Next.js App Router guardrails."
